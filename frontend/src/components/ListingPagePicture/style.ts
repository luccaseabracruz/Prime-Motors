import styled from 'styled-components';

export const StyledListPicture = styled.li`
  li {
    background-color: var(--color-grey-7);
    cursor: pointer;
  }
  li > img {
    max-width: 100%;
  }
`;
